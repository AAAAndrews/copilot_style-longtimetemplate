---
description: IDE agent workflow for incremental, test-first, traceable progress
globs:
  - "**/*"
alwaysApply: true
---

# Cursor Agent 工作流规则

遵循 `AGENTS.md` 中定义的仓库工作流。

## 必须行为

1. 先运行 `./scripts/init-session.sh`。
2. 阅读 `docs/task-list.json` 并只选择一个未完成任务。
3. 仅实现该任务。
4. 宣布完成前必须运行相关检查。
5. 在 `docs/task-list.json` 中仅更新该任务允许变更的状态字段。
6. 在 `docs/progress.md` 追加结构化交接记录。
7. 以任务范围为单位提交。

## 约束

- 禁止无说明跳过任务。
- 无检查证据不得标记完成。
- 禁止无关重构。
- 每次会话结束都要保持可合并状态。

## 若发生阻塞

将阻塞细节记录到 `docs/progress.md` 后停止，不得将任务标记为完成。
